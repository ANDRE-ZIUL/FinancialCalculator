import React from "react";
import ReactDOM from "react-dom/client";
import { FinancialCalculator } from "./Interface";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <FinancialCalculator />
  </React.StrictMode>
);
